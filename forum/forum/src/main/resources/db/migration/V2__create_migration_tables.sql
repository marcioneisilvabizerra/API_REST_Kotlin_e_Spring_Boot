CREATE TABLE curso (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   categoria VARCHAR(255),
   nome VARCHAR(255),
   CONSTRAINT curso_pkey PRIMARY KEY (id)
);

CREATE TABLE resposta (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   data_criacao TIMESTAMP WITHOUT TIME ZONE,
   mensagem VARCHAR(255),
   solucao BOOLEAN NOT NULL,
   autor_id BIGINT,
   topico_id BIGINT,
   CONSTRAINT resposta_pkey PRIMARY KEY (id)
);

CREATE TABLE topico (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   data_criacao TIMESTAMP WITHOUT TIME ZONE,
   mensagem VARCHAR(255),
   status VARCHAR(255),
   titulo VARCHAR(255),
   autor_id BIGINT,
   curso_id BIGINT,
   CONSTRAINT topico_pkey PRIMARY KEY (id)
);

CREATE TABLE usuario (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   email VARCHAR(255),
   nome VARCHAR(255),
   CONSTRAINT usuario_pkey PRIMARY KEY (id)
);

ALTER TABLE resposta ADD CONSTRAINT fk9999kvnmdq63ah7imctrl06r7 FOREIGN KEY (autor_id) REFERENCES usuario (id) ON UPDATE NO ACTION ON DELETE NO ACTION;

ALTER TABLE topico ADD CONSTRAINT fkcaaogjo0ynd54updie6kdpxd1 FOREIGN KEY (curso_id) REFERENCES curso (id) ON UPDATE NO ACTION ON DELETE NO ACTION;

ALTER TABLE resposta ADD CONSTRAINT fkltuv9rkfjtlmn8b0rb3wdbjsv FOREIGN KEY (topico_id) REFERENCES topico (id) ON UPDATE NO ACTION ON DELETE NO ACTION;

ALTER TABLE topico ADD CONSTRAINT fksk04hscorwqdymnafg8882v64 FOREIGN KEY (autor_id) REFERENCES usuario (id) ON UPDATE NO ACTION ON DELETE NO ACTION;